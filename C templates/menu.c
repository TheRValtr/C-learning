do {
    printMenu();
    scanf("%d", &choice);

    switch (choice) {
        case 1:
            // action
            break;
        case 0:
            // exit
            break;
    }
} while (choice != 0);